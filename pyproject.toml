[project]
name = "ananas"
version = "0.1.0"
description = "Pinapple"
readme = "README.md"
requires-python = ">=3.13"

dependencies = [
    "camoufox[geoip]>=0.4.11",
    "playwright",
    "python-dotenv>=1.2.1",
    "ruff",
]
